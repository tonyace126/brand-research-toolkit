# 複製成 .env 並填入你自己的值。.env 絕對不要 commit（記得加進 .gitignore）。

# Slack Incoming Webhook URL（去 Slack → Apps → Incoming Webhooks，挑「東尼大木羅德島」頻道建立）
SLACK_WEBHOOK_URL=

# 安全開關：預設不設 = dry-run（只印不發）。確認沒問題後才設 SLACK_LIVE=1 真發。
# SLACK_LIVE=1
